spring:
  application:
    name: listing-assistant

  ai:
    vertex:
      ai:
        gemini:
          api-key: ${GEMINI_API_KEY:AQ.mock-key}
          model-name: gemini-1.5-flash
          chat:
            options:
              model: gemini-1.5-flash
              temperature: 0.7

app:
  ai:
    mock-mode: ${MOCK_MODE:true}

